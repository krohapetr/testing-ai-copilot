[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pos-extractor"
version = "0.1.0"
description = "CLI tool to extract nouns and verbs from text files"
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Testing AI Copilot" }
]
dependencies = [
  "spacy>=3.7,<4.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[project.scripts]
pos-extract = "pos_extractor.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
